import 'package:flutter/material.dart';

class YYYY extends StatelessWidget {
  const YYYY({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}